{
  "name": "aylla-imports",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Sistema de importacao simplificada e revenda em marketplace",
  "scripts": {
    "dev": "vite",
    "build": "npm test && node scripts/gerar-icones.mjs && vite build",
    "preview": "vite preview",
    "icones": "node scripts/gerar-icones.mjs",
    "test": "node --test test/*.test.js"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^5.2.0",
    "vite": "^6.4.3"
  }
}
